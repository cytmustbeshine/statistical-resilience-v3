# A10 成功方案过程说明

## 1. 结论先说

截至 2026 年 8 月 5 日，A10 在预先冻结的 PEMS07 外部确认协议上通过了普通预测和统计韧性两道门槛，因此被选为当前项目的确认成功方案。

这里的“成功”有严格范围：它证明 A10 在 PEMS07 的首 41 个节点、严格时间切分和三种随机种子下优于同配置的公开代码改编 DCRNN；不表示它已经在所有数据集、所有事件类型或所有交通任务上全面超过所有公开基线。

## 2. 为什么前面的方案失败

项目先后尝试了更复杂的解码器、课程学习、统计门控多专家和统计身份残差模型。它们有些在验证集改善，但在未参与选择的新外部数据上失效。根本教训是：验证集上的优势不能替代冻结外部确认，辅助韧性头的输出也不能替代物理交通预测结果。

因此 A10 没有继续堆叠图网络，而是保留公开 DCRNN 深度骨架，把贡献放在可解释、可审计的统计后处理层。

## 3. A10 做了什么

A10 = 公开代码改编 DCRNN + 分层统计残差校准：

1. DCRNN 输出物理流量预测；
2. 用最后一个观测值构造持久性预测；
3. 在验证期比较五个冻结混合权重：0.85、0.90、0.95、1.00、1.05；
4. 对每个节点×预测步计算验证期中位残差；
5. 只施加 25% 的中位残差，避免过度校准；
6. 对结果施加非负物理裁剪；
7. 权重只由验证期四项指标的最大相对比率和平均相对比率选择。

所有校准量都在测试集打开前完成并冻结。

## 4. PEMS07 数据与隔离

- 数据文件：`D:\TrafficGNN\data\public\PEMS07\PEMS07.npz`
- 形状：28224 个时间点×883 个节点×1 个流量变量
- 实验节点：NPZ 顺序中的前 41 个节点
- 文件大小：43705518 字节
- MD5：`978d3d9b85fe640a446983a34271a48d`
- 历史窗口：12；预测步长：12
- 时间切分：严格按时间 60%/20%/20%，目标时间不重叠
- 训练和校准阶段：不构造测试加载器，也不生成测试预测
- 测试解锁前：3 个 DCRNN 检查点、3 个校准档案、参数和文件哈希审计均通过

PEMS07 的完整日历信息没有作为可信输入使用。韧性统计只使用观测序列索引 `index mod 288` 表示五分钟日内槽位，并关闭工作日/周末分组，没有伪造具体日期。

## 5. 普通预测结果

三种子为 42、2024、3407。A10 相对 DCRNN 的平均比率如下；小于 1 表示 A10 误差更低：

| 指标 | A10/DCRNN 平均比率 | 胜出数 |
|---|---:|---:|
| MAE | 0.988701 | 3/3 |
| RMSE | 0.995057 | 3/3 |
| SMAPE | 0.952364 | 3/3 |
| WAPE | 0.988701 | 3/3 |

1000 次、块长度 12 的 moving-block bootstrap 在 12 个种子×指标比较中全部得到严格改善区间，预测步 MAE 比较为 36/36 改善。由于误差具有明显时间相关性，不能把这些点视为 iid 样本。

## 6. 统计韧性结果

韧性评价使用训练期拟合的分层条件 ECDF、训练期 deficit clipping 值和训练期 q90 阈值。比较的是物理预测经过同一个冻结统计变换后的流量 deficit。

| 韧性指标 | A10/DCRNN 平均比率 | 胜出数 |
|---|---:|---:|
| continuous deficit MAE | 0.947488 | 3/3 |
| continuous deficit RMSE | 0.928830 | 3/3 |
| q90-tail deficit MAE | 0.894058 | 3/3 |

q90 高状态 F1 均值：A10 为 0.717946，DCRNN 为 0.689878；因此没有降低平均高状态识别质量。三种子各自的 test target count 均为 5645。

韧性误差残差 lag-1 相关仍约为 0.72–0.87，所以采用 moving-block bootstrap 是统计上必要的，而不是装饰性分析。

## 7. 可复现实验入口

```powershell
& 'D:\soft\Python310\python.exe' run_a10_pems07_external_confirmation.py `
  --stage evaluate `
  --output-dir 'D:\TrafficGNN\outputs\a10_pems07_external_confirmation' `
  --seeds '42,2024,3407' `
  --dcrnn-epochs 20

& 'D:\soft\Python310\python.exe' analyze_a10_pems07_external.py `
  --root 'D:\TrafficGNN\outputs\a10_pems07_external_confirmation' `
  --bootstrap-repetitions 1000 `
  --block-length 12 `
  --seed 42

& 'D:\soft\Python310\python.exe' analyze_a10_pems07_resilience.py `
  --root 'D:\TrafficGNN\outputs\a10_pems07_external_confirmation' `
  --bootstrap-repetitions 1000 `
  --block-length 12 `
  --seed 42
```

## 8. 关键输出

- `D:\TrafficGNN\outputs\a10_pems07_external_confirmation\a10_final_decision.json`
- `D:\TrafficGNN\outputs\a10_pems07_external_confirmation\a10_vs_dcrnn_external_verified.csv`
- `D:\TrafficGNN\outputs\a10_pems07_external_confirmation\a10_external_block_bootstrap.csv`
- `D:\TrafficGNN\outputs\a10_pems07_external_confirmation\a10_resilience_metrics.csv`
- `D:\TrafficGNN\outputs\a10_pems07_external_confirmation\a10_resilience_block_bootstrap.csv`
- `D:\TrafficGNN\outputs\a10_pems07_external_confirmation\a10_resilience_residual_dependence.csv`

## 9. 论文表述边界

论文中应称 A10 为“公开 DCRNN 深度骨架上的分层统计残差校准方法”。统计学贡献是 train-only 校准、时间相关不确定性评估和统一 deficit 后处理；不能把动态图或校准层直接解释成因果交通韧性，也不能把 PEMS07 的流量 deficit 直接等同于真实事故事件。
